
NAME
    ceph container list - List containers on remote CEPH storage. 

SYNOPSIS
    ceph container list --connection <pathname>

OPTIONS
    --connection | -c <pathname>
    CEPH storage connection configuration file.
